Простой VNC-сервер с управлением (RFC 6143)

Поддерживает только raw-encoding и не поддерживает аутентификацию.

Запуск из консоли:
`./gradlew run`

Остановка сервера:
`/exit`

Сервер запускается на порту 5900
